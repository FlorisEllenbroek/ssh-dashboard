{
  "name": "ssh-dashboard",
  "version": "1.0.0",
  "description": "Web-based terminal dashboard with system info",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "hash": "node -e \"const bcrypt = require('bcrypt'); const pw = process.argv[1]; bcrypt.hash(pw, 10).then(h => console.log(h));\" --"
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "express-session": "^1.18.0",
    "node-pty": "^1.0.0",
    "systeminformation": "^5.22.0",
    "ws": "^8.17.0"
  }
}
