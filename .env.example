# Initial admin credentials (used to bootstrap first admin user)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=changeme

# Session secret (use a long random string)
SESSION_SECRET=change-me-to-a-random-string

# Server port
PORT=3000
